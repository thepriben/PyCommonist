# Runtime dependencies (also declared in pyproject.toml)
PyQt6>=6.7.1
requests>=2.32.0
ExifRead>=3.0.0
PyYAML>=6.0
Send2Trash>=1.8.0

# Development / release
# pip install -e ".[dev]"  — see pyproject.toml optional deps
build>=1.2.0
twine>=5.0.0
